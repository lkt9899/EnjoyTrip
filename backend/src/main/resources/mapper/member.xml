<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.member.model.dao.MemberRepository">

	<resultMap id="memberInfo" type="Member">
		<id property="id" column="id"/>
		<result property="password" column="password"/>
		<result property="name" column="name"/>
		<result property="age" column="age"/>
		<result property="phoneNumber" column="phone_number"/>
		<result property="gender" column="gender"/>
	</resultMap>

	<insert id="register" parameterType="Member">
		insert into member (id, password, name, phone_number, gender, age)
		values (#{id}, #{password}, #{name}, #{phoneNumber}, #{gender}, #{age})
	</insert>

	<select id="selectMemberByMemberId" parameterType="int" resultType="memberInfoResponse">
		select member_id as memberId, id as id, password as password, name as name, phone_number as phoneNumber, gender as gender, age as age, reg_date as regDate, mod_date as modDate
		from member
		where member_id = #{member_id}
	</select>

	<select id="selectMemberById" parameterType="String" resultType="memberInfoResponse">
		select member_id as memberId, id as id, password as password, name as name, phone_number as phoneNumber, gender as gender, age as age, reg_date as regDate, mod_date as modDate
		from member
		where id = #{id}
	</select>

	<update id="update" parameterType="memberUpdateRequest">
		update member set
						  password = #{password},
						  name = #{name},
						  phone_number = #{phoneNumber},
						  gender = #{gender},
						  age = #{age},
						  mod_date = now()
		where id = #{id}
	</update>

	<delete id="delete" parameterType="int">
		delete FROM member
		WHERE member_id = #{memberId}
	</delete>
	

</mapper>